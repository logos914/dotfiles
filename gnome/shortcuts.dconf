# Placeholder. Populate this file by running `bin/dot gnome backup`
# on a machine that already has your preferred GNOME shortcuts.
# dconf treats lines starting with `#` as comments and the rest as key bindings.